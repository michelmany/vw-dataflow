# Environment Configuration
# Copy this file to .env.local for local development

# Development API URL (used by JSON Server)
VITE_API_URL=http://localhost:3001/api

# Production API URL (will be set automatically by Vercel)
# VITE_API_URL=/api

# Vercel Environment Variables (automatically set by Vercel)
# VERCEL_URL=your-app-url.vercel.app
# VERCEL_ENV=development|preview|production

# Upstash Redis Environment Variables
# Get these from your Upstash dashboard after creating a Redis database
# UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your_redis_token

# Legacy Vercel KV Environment Variables (if using Vercel KV instead)
# These will be automatically set by Vercel when you enable KV storage from Marketplace
# KV_REST_API_URL=your_kv_rest_api_url
# KV_REST_API_TOKEN=your_kv_rest_api_token
# KV_REST_API_READ_ONLY_TOKEN=your_kv_rest_api_read_only_token

# For local development with KV, run: vercel env pull .env.local
